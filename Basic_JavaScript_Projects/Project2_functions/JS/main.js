function myFunction() {   //naming the function
    var sentence = "I am learning"; //declaring a variable
    sentence += " a lot from this book!"; //concatenating something to the variable
    document.getElementById("Concatenate").innerHTML = sentence; //displaying the result
}